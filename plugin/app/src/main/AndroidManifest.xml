<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:label="MobileGlues-Adreno">

        <!--
            FCL / ZalithLauncher renderer plugin identification.
            The launcher scans installed APKs for fclPlugin=true and reads
            the rest of the meta-data to populate its renderer list.
        -->
        <meta-data android:name="fclPlugin"  android:value="true" />
        <meta-data android:name="des"        android:value="${des}" />
        <meta-data android:name="renderer"   android:value="${renderer}" />
        <meta-data android:name="boatEnv"    android:value="${boatEnv}" />
        <meta-data android:name="pojavEnv"   android:value="${pojavEnv}" />
        <meta-data android:name="minMCVer"   android:value="${minMCVer}" />
        <meta-data android:name="maxMCVer"   android:value="${maxMCVer}" />

        <!-- Minimal launcher activity so the APK is installable -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
